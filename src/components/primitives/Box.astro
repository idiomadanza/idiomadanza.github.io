---
interface Props {
  as?: any;
  padding?: string;
  margin?: string;
  class?: string;
}

const { as: Tag = 'div', padding = 'p-fluid-gutter', margin, class: className } = Astro.props;
---

<Tag class:list={[padding, margin, className]}>
  <slot />
</Tag>
